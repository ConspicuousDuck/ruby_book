# arrays_ex06.rb

=begin

The problem is that there is a string ('margaret') where an integer should be.
You can fix it by accessing the element correctly like so: names[3] = 'jody'
OR if you don't know the index
names[names.index('margaret')] = 'jody'

=end
